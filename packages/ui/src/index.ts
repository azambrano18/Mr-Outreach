/**
 * Placeholder package. Will hold shared React components starting when
 * the first real screens are built (executives, mailboxes, templates).
 * Kept empty of components in Phase 0 on purpose.
 */
export const UI_PACKAGE_VERSION = '0.1.0';
